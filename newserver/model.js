// const pool = require('../postgresql/index.js');

// const getRestaurants = (cb) => {
//   const q = 'SELECT * FROM subtest2';
//   pool.query(q, cb);
// };

// const addRestaurants = (name, age, cb) => {
//   const q = 'INSERT INTO subtest2 values(default, ?, ?)';
//   pool.query(q, [name, age], cb);
// };

// module.exports = {
//   getRestaurants,
//   addRestaurants,
// };
